---
"@sophys-web/pvws-store": minor
---

- Improved docs with guide on how to use the `@sophys-web/pvws-store` with side effects and to clarify the use cases for the `usePvData` and `usePvDataMap` hooks.

- Deprecated warning for the `useSinglePvData` hook name, which is being renamed to `usePvData` to better reflect its purpose and prevent confusion.
